define(['knockout', 'text!./ListGrid.html'], function (ko, template) {
    var listGridViewModel = function () {
        var model = this;

    };

    return { viewModel: listGridViewModel, template: template };
});